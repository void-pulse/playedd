# Animated A/B variant — build log

Animated version of ep6 teaser short #1 (the reframe), to A/B against the static
`shorts/01_reframe/01_reframe_short.mp4`. Same beat, same VO script, same CTA — only
the visuals differ (Kling-animated vs static stills), so the test isolates motion.

- Pipeline: 6 portrait doodle stills -> Kling v2.1 **standard** image-to-video (1080p target, NOT 4K) -> stitched to Jack VO + ducked ep6 score, full-bleed 1080x1920.
- Shot 2 (the couch-flop "money shot") read clean in standard, so **no Pro tier needed**.
- **Animation cost (estimate): ~$1.5** (6 standard clips x ~5s; standard ~$0.05/s, audio-off). No Pro, no 4K.
- Output: `01_reframe_animated_short.mp4` (24.6s).
- CTA matches the current static short (logo + arrow + FOR FULL BREAKDOWN + "LAZY CHALLENGE") so the A/B compares animation, not end cards. (Spec said "up-arrow"; used the current standard card for A/B parity.)
